<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head.ejs')%>
  </head>
  <body>
    <div class="container">
      <div id="pokedex-container">
        <form action="/pokemon/<%=index%>?_method=PUT" method="POST">
          <h1>Edit Pok&eacute;mon</h1>
          <p>
            <label for="name">Name:</label>
            <input type="text" name="name" value="<%= pokemon.name %>" />
          </p>
          <p>
            <label for="img">Image(URL):</label>
            <input type="text" name="img" value="<%= pokemon.img.src %>" />
          </p>
          <p>
            <label for="type">Type(s):</label>
            <input type="text" name="type" value="<%= pokemon.type[0] %>" />
            <input type="text" name="type" value="<%= pokemon.type[1] %>" />
            <input type="text" name="type" value="<%= pokemon.type[2] %>" />
          </p>
          <p>
            <label for="hp">Health Points:</label>
            <input type="text" name="stats" value="<%= pokemon.stats.hp %>" />
          </p>
          <p>
            <label for="attack">Attack:</label>
            <input
              type="text"
              name="stats"
              value="<%= pokemon.stats.attack %>"
            />
          </p>
          <p>
            <label for="defense">Defense:</label>
            <input
              type="text"
              name="stats"
              value="<%= pokemon.stats.attack %>"
            />
          </p>
          <input value="Edit Pok&eacute;mon" type="submit" />
        </form>
      </div>
    </div>
  </body>
</html>
